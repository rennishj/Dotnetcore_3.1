{
  "openapi": "3.0.1",
  "info": {
    "title": "Movies API",
    "version": "1.0"
  },
  "paths": {
    "/api/movies": {
      "post": {
        "tags": [
          "Movies"
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MovieForCreation"
              }
            },
            "text/json": {
              "schema": {
                "$ref": "#/components/schemas/MovieForCreation"
              }
            },
            "application/*+json": {
              "schema": {
                "$ref": "#/components/schemas/MovieForCreation"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Success"
          }
        }
      }
    },
    "/api/movies/getallmovies": {
      "get": {
        "tags": [
          "Movies"
        ],
        "responses": {
          "200": {
            "description": "Success"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "MovieForCreation": {
        "required": [
          "description",
          "director",
          "genre",
          "releaseDate",
          "title"
        ],
        "type": "object",
        "properties": {
          "title": {
            "maxLength": 500,
            "type": "string"
          },
          "description": {
            "maxLength": 500,
            "type": "string"
          },
          "genre": {
            "maxLength": 10,
            "type": "string"
          },
          "releaseDate": {
            "type": "string",
            "format": "date-time"
          },
          "director": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    }
  }
}